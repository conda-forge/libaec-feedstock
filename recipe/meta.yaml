{% set version = "1.1.5" %}

package:
  name: libaec
  version: {{ version }}

source:
  url: https://github.com/Deutsches-Klimarechenzentrum/libaec/releases/download/v{{ version }}/libaec-{{ version }}.tar.gz
  sha256: 70c2c5909523de64ab9f9cbbb5c28df7a91e2b6c7102f0dea8f2e3a940c35122

build:
  number: 1
  run_exports:
    - {{ pin_subpackage('libaec') }}

requirements:
  build:
    - cmake
    - make     # [not win]
    - ninja    # [win]
    - {{ compiler('c') }}
    - {{ stdlib('c') }}
    - {{ compiler('cxx') }}

test:
  requires:
    - cmake  # [target_platform == "win-arm64"]
    - {{ compiler('c') }}  # [target_platform == "win-arm64"]
  files:
    - CMakeLists.txt  # [target_platform == "win-arm64"]
    - test_aec.c  # [target_platform == "win-arm64"]
    - test_sz.c  # [target_platform == "win-arm64"]
  commands:
    - test -f ${PREFIX}/lib/libaec${SHLIB_EXT}        # [not win]
    - test -f ${PREFIX}/lib/libsz${SHLIB_EXT}         # [not win]
    - if not exist %LIBRARY_LIB%\\aec.lib exit 1      # [win]
    - if not exist %LIBRARY_BIN%\\aec.dll exit 1      # [win]
    - if not exist %LIBRARY_LIB%\\szip.lib exit 1     # [win]
    - if not exist %LIBRARY_BIN%\\szip.dll exit 1     # [win]
    - if not exist %LIBRARY_INC%\\libaec.h exit 1     # [win]
    - cmake -S . -B test-build -G "NMake Makefiles"  # [target_platform == "win-arm64"]
    - if errorlevel 1 exit 1  # [target_platform == "win-arm64"]
    - cmake --build test-build --config Release  # [target_platform == "win-arm64"]
    - if errorlevel 1 exit 1  # [target_platform == "win-arm64"]
    - test-build\aec_consumer.exe  # [target_platform == "win-arm64"]
    - if errorlevel 1 exit 1  # [target_platform == "win-arm64"]
    - test-build\sz_consumer.exe  # [target_platform == "win-arm64"]
    - if errorlevel 1 exit 1  # [target_platform == "win-arm64"]
    - dumpbin /headers test-build\aec_consumer.exe | findstr /I /C:"AA64 machine (ARM64)"  # [target_platform == "win-arm64"]
    - if errorlevel 1 exit 1  # [target_platform == "win-arm64"]
    - dumpbin /headers test-build\sz_consumer.exe | findstr /I /C:"AA64 machine (ARM64)"  # [target_platform == "win-arm64"]
    - if errorlevel 1 exit 1  # [target_platform == "win-arm64"]
    - dumpbin /headers "%LIBRARY_BIN%\\aec.dll" | findstr /I /C:"AA64 machine (ARM64)"  # [target_platform == "win-arm64"]
    - if errorlevel 1 exit 1  # [target_platform == "win-arm64"]
    - dumpbin /headers "%LIBRARY_BIN%\\szip.dll" | findstr /I /C:"AA64 machine (ARM64)"  # [target_platform == "win-arm64"]
    - if errorlevel 1 exit 1  # [target_platform == "win-arm64"]

about:
  home: https://gitlab.dkrz.de/k202009/libaec
  license: BSD-2-Clause
  license_family: BSD
  license_file: LICENSE.txt
  summary: Adaptive Entropy Coding library
  description: |
    Libaec provides fast lossless compression of 1 up to 32 bit wide signed or unsigned integers (samples).
    The library achieves best results for low entropy data as often encountered in space imaging instrument
    data or numerical model output from weather or climate simulations. While floating point representations
    are not directly supported, they can also be efficiently coded by grouping exponents and mantissa.
    Libaec implements Golomb-Rice coding as defined in the Space Data System Standard documents 121.0-B-2
    and 120.0-G-2.
  dev_url: https://gitlab.dkrz.de/k202009/libaec

extra:
  recipe-maintainers:
    - kmuehlbauer
    - hmaarrfk
